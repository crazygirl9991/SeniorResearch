
	// take downloadStore and open *.fit (read)
	// TODO implement this (shouldn't be too hard but need to modify DownloadStore)
	
	// load info in table (write)
	// TODO which columns?
	
	// check one at a time for similarities and list those in table (somehow)
	//TODO how to list that multiple files are for the same object in table
	//TODO how to calculate multiple files are for same object
